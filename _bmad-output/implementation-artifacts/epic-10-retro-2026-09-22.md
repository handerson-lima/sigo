---
epic: 10
date: 09-22-2026
verdict: accepted-with-open-items
criteria: declared
headless: false
---

# Retrospectiva — Epic 10: Gestão do vínculo

## Epic summary

- **Epic:** 10 — Gestão do vínculo (FR6, FR7, FR8, FR9 de `epics.md`).
- **Diff range:** `55fabb8..ba99902` (parent do primeiro commit do épica até o último). 5 commits, 0 merges:
  - `9a86836` spec-epic-10 — `epic-10-context.md` + esqueleto da spec 10.1/10.2 (sem código)
  - `5fb2602` spec-10-1-2 — **implementação 10.1+10.2** (`trocar_papel_dialog.dart` +352, `obra_vinculo_row.dart` +45, `member_detalhe_sheet.dart` +115, `membros_test.dart` +325)
  - `4ee0852` spec-10-3-trocar-cargo — spec 10.3 + reformat em massa de `membros_test.dart` (1256+/920−, majoritariamente `dart format`)
  - `959d669` implementa-10-3-troca-cargo-na-construtora-e-desativar — **implementação 10.3** (`trocar_cargo_dialog.dart` +277, `membros_repository.dart` +62, `member_detalhe_sheet.dart` +243, `membros_test.dart` +459)
  - `ba99902` chore(sprint-status) — fecha status épicos 8 e 10 (só docs)
- **Atribuição por story (manual):** `git_evidence.py stories` retornou `[]` para os 3 ids — os subjects não contêm as chaves do sprint-status (ex.: `spec-10-1-2` ≠ `10-1-trocar-papel-modulos-na-obra`). Mapeamento manual: 10.1+10.2 → `5fb2602` (uma única sessão para duas stories); 10.3 → `959d669`.
- **Stories concluídas (todas `done`):**
  - `10-1-trocar-papel-modulos-na-obra` — baseline `9a86836`; entregue junto com 10.2 em `5fb2602`
  - `10-2-remover-da-obra` — mesma spec (`spec-10-1-2…`) e mesmo commit que 10.1
  - `10-3-trocar-cargo-na-construtora-e-desativar` — baseline `4ee0852`; implementada em `959d669`
- **Pending stories:** nenhuma (`pending_stories = []` via `detect-epic --epic 10`; `story_count: 3`).
- **Volume de código (não-merge, top churn):**
  - `app/test/features/construtoras/membros_test.dart` +1019 net (2351 → 3370 linhas)
  - `app/lib/.../member_detalhe_sheet.dart` +324 net (→ 756 linhas)
  - `app/lib/.../widgets/trocar_papel_dialog.dart` +352 (novo)
  - `app/lib/.../widgets/trocar_cargo_dialog.dart` +277 (novo)
  - `app/lib/.../widgets/obra_vinculo_row.dart` +43
  - `app/lib/.../construtoras/data/membros_repository.dart` +62 (`setCargo`)
- **Inventário de evidências:**
  - Epic context: `epic-10-context.md` (presente).
  - Specs de story: `spec-10-1-2-trocar-papel-remover-obra.md` (10.1+10.2 combinadas, `done`, intent frozen), `spec-10-3-trocar-cargo-na-construtora-e-desativar.md` (`done`, intent frozen) — presentes.
  - Architecture: `architecture/architecture-obras-2026-09-21/ARCHITECTURE-SPINE.md` (AD-1…AD-9).
  - UX: `ux-designs/ux-obras-2026-09-21/DESIGN.md` e `EXPERIENCE.md` (referenciados pelo spine).
  - Epics/ACs: `planning-artifacts/epics.md` (Stories 10.1–10.3 Given/When/Then; FR6–FR9).
  - Sprint status: `sprint-status.yaml` (epic-10 `done`; retrospectiva `optional` antes desta run).
  - Retro anterior direta: `epic-9-retro-2026-09-22.md` (3 itens open) + `epic-8-retro-2026-09-22.md` (3 itens open).
  - Diff epic-wide gerado nesta run: `diff-epic-10.diff` (4836 linhas).
  - **Session logs / memlogs das stories 10.x:** ausentes — análise de processo pulada (escopo estreitado; evidência = specs + commits + código).
  - Diffs por story (`diff-spec-10-*.diff`): ausentes — range epic-wide usado no lugar.
  - `functions/` intocado no range — CFs `setMembership`/`setConstrutoraRole` pré-existentes, sem mudança de backend neste épica.
  - PRD: ausente — critérios vêm das ACs de `epics.md` e das specs congeladas (**declared**).

## Findings

Fonte primária reaberta para cada achado de sub-agente antes de roteirizar (regra da Fase 4). Overlap entre lentes é sinal, não duplicata.

### Aggregate views

1. **Architecture delta — aderência AD-1/AD-3/AD-7/AD-8, sem escrita direta** *(Source: ARCHITECTURE-SPINE.md:39-91; diff range; `functions/` intocado)*
   - UI → dialogs → `ObraMembersRepository.setMembership` / `MembrosRepository.setCargo` → `httpsCallable`; nenhum `FirebaseFirestore` de escrita na camada cliente; `owner` na obra nunca sai da UI; desativação segue N+1 sem cascata (AD-7). Fronteiras do range ficam em `construtoras/presentation`, `construtoras/data`, `obras/data` e testes.
   - **Disposition:** accept.

2. **Duplication map — rótulos, erros e status-chip** *(Source: `trocar_papel_dialog.dart:71-74`; `trocar_cargo_dialog.dart:64-68`; `atribuir_obra_dialog.dart:65-70`; `membros_providers.dart:330`; `membros_repository.dart:7-23,103-133`; `obra_members_repository.dart:5-29`; `obra_vinculo_row.dart:61-63,134-157`; `member_detalhe_sheet.dart:343-376`)*
   - `_roleLabels` agora em **3 dialogs** + `rotuloPapelObra` — o item `epic-9-retro-item-2` pediu unificação; o épica 10 **criou uma 3ª cópia** em vez de unificar.
   - `mensagemSemConexaoCargo` é cópia byte-a-byte de `mensagemSemConexao`; `traduzirErroSetCargo` reimplementa `traduzirErroSetMembership` com `failed-precondition` **divergente** (`Ative na construtora primeiro.` vs `Pré-condição não atendida: …` — a tabela AD/Consistency diz a forma curta; a spec 9.2 congelou a longa).
   - Tokens do status-chip duplicados entre `_statusChip` (sheet) e o chip inline de `ObraVinculoRow` — o arquivo `obra_vinculo_row.dart` **foi editado neste épica** (overflow) sem resolver `epic-8-retro-item-2`.
   - Fallback `email ou 'UID: …'` repetido em 5 pontos do sheet. Offline-check reimplementada em `setCargo` sem seam injetável (diverge de `ObraMembersRepository`).
   - **Disposition:** fix now (item de consolidação) + manter `epic-9-retro-item-2` open.

3. **God-class / crescimento — `membros_test.dart` e `member_detalhe_sheet.dart`** *(Source: `git_evidence` files; `wc -l`)*
   - `membros_test.dart` 2351 → **3370** (+1019 net; +658 no épica 9, +… no 8) — terceiro épica consecutivo ignorando a lição de fatiar (epic-1/epic-9).
   - `member_detalhe_sheet.dart` → **756** linhas (+324), agora hospeda 5 fluxos de ação (atribuir, trocar papel, remover, trocar cargo, desativar); doc comment ainda diz "somente leitura … ações dos epics 9/10 ficam desabilitadas" (`member_detalhe_sheet.dart:18-22`).
   - Diálogos novos (352 + 277) são clones estruturais entre si e do `AtribuirObraDialog` — padrão clone-and-own.
   - **Disposition:** accept com lição de processo recorrente (ação de fatiar testes, desta vez explícita); doc comment → fix now (trivial).

4. **Pattern divergence — seams e commits** *(Source: `membros_repository.dart:105,111,129`; `obra_members_repository.dart:39-46`; commit `4ee0852`)*
   - `Connectivity()` hardcoded + rethrow por `toString().contains(mensagem)` em `setCargo` (frágil; o repo de obra tem seam injetável).
   - `'$e'.contains('SocketException')` copiado da 9.2 (já aceito na retro 9, agora com 2ª cópia).
   - Commit `4ee0852` ("spec-10-3") mistura spec com reformat em massa de `membros_test.dart` (1256+/920−) — concerns misturados inflam o churn do range.
   - Contrato de dialog divergente na fronteira 10.1/10.2 ↔ 10.3: `TrocarPapelDialog` popa e snacka a si mesmo (context pós-pop); `TrocarCargoDialog` retorna `bool` e o chamador snacka; `_mostrarTrocarPapelDialog` recebe `ref` nunca usado (`member_detalhe_sheet.dart:470`).
   - **Disposition:** defer (unificar seam de offline/erros no item de consolidação); doc comment e `ref` morto → fix now.

5. **Spec-to-implementation reconciliation** *(Source: `epics.md:166-204`; specs frozen; código)*
   - a) **AC 10.3**: `epics.md:203` prevê confirm `Remover de N obras + desativar?`; código abre `Desativar {identificador}?` com lista de obras (`member_detalhe_sheet.dart:558`). Spec frozen não fixa o título — divergência epics↔código.
   - b) **I/O 10.3 "sem obras"**: spec congela `Confirm lista "nenhuma obra vinculada"`; branch existe (`:546-553`) mas o render é gateado em `obrasAfetadas.isNotEmpty` (`:568`) — **branch morta, critério não exibido**.
   - c) **AC 10.1 "módulos normalizam legados"**: prefill usa `contains('diario')` cru (`trocar_papel_dialog.dart:91-96`) sem `normalizeModule` (`core/contracts.dart:3-16`); vínculo com `rdo`/`almoxarifado` abre com "nenhum módulo" e confirmar sem alterar **revoga** acesso (o resto do app normaliza na leitura: `access_guard.dart:55-68`, sidebar, dashboard). Backend faz só dedupe (`index.ts:102`), não normaliza.
   - d) **AC 10.2 "Acesso removido"**: string existe só em `sync_queue_screen.dart:26` (pré-épica, label `authorization_rejected`) — comportamento de gate não exercitado nesta retro (*nunca checado em runtime*, não "checado e limpo").
   - e) **Carryover 9.2**: atalho Ativar (AD-9 + `epics.md:159`) continuou sem implementação; `epics.md` intocado no range (diff vazio).
   - f) Doc comment do sheet (ver finding 3) e chaves de story ausentes nos subjects (inventário).
   - **Disposition:** (b) e (c) fix now como defeito/spec reconciliation; (a),(e) spec reconciliation para decisão humana; (d) registrado como escopo não exercitado.

### Diff-scope review (lentes `bmad-review` sobre `diff-epic-10.diff`)

Lentes independentes: adversarial (16 findings), edge-case-hunter (12), verification-gap (7). Overlaps consolidados; каждый re-verificado no código/`functions` antes de roteirizar.

**Defeitos de código (verificados — fix now):**

6. **Reativação silenciosa via "Trocar cargo" em membro inativo** *(Source: `member_detalhe_sheet.dart:192-201` — habilita com `vinculo != null`; `trocar_cargo_dialog.dart:94-98` — `setCargo` sem `isActive`; `membros_repository.dart:101` — default `isActive=true`; backend `functions/src/index.ts:103` — `isActive: d.isActive !== false`)* — Confirmar num membro inativo reativa o vínculo da construtora sem intenção explícita (os botões Atribuir/Desativar exigem `isActive==true`; Trocar cargo não). **Adversarial+edge confirmados.**
7. **Fallback `owner → operario` no `TrocarCargoDialog`** *(Source: `trocar_cargo_dialog.dart:79-82`)* — `cargoAtual='owner'` com `isDev=false` cai em `'operario'` pré-selecionado ("será Operário"). Backend mitiga (`index.ts:96` → `permission-denied` para não-dev), mas a UI mente e o confirm é um erro garantido. **Ambas as lentes.**
8. **Prefill de módulos sem normalização legada** — ver finding 5(c). **Ambas as lentes.**
9. **Desativar um `owner` falha no passo final após N obras** *(Source: `member_detalhe_sheet.dart:626-654`; `index.ts:96` — `existing?.isOwner && !a.dev` → `permission-denied`)* — N `setMembership` rodam, `setCargo` final é rejeitado sem `trustedDev`: estado parcial com erro; e a invalidação (`:656-662`) fica **dentro do `try` após `setCargo`** — nunca roda em falha parcial; `obraMembersProvider` das obras afetadas nunca é invalidado nos 4 fluxos. **Edge+adversarial confirmados.**
10. **"Desativar" decide com lista de obras degradada** *(Source: `member_detalhe_sheet.dart:210-218`; `membros_providers.dart:252-300` — `.value ?? const []` em loading/erro)* — diálogo destrutivo pode listar "Nenhuma obra"/omitir obras reais; `meta` já está em escopo (`:91`) e não é usado como guarda. Impacto de acesso temperado pelo gate AD-7, mas a decisão é tomada com dado errado. **Ambas as lentes.**
11. **Branch morta "Nenhuma obra vinculada"** — ver finding 5(b). **Edge.**
12. **Overflow 32×32dp + `excludeSemantics` engole o menu** *(Source: `obra_vinculo_row.dart:69-71,90-98` vs spec 10.1 Always ≥48dp e `semanticsLabel` cobrindo ação)* — alvo abaixo do mínimo declarado; leitor de tela não expõe "Trocar papel"/"Remover da obra" como ações. **Três lentes (adv, edge, VG).**
13. **Sem lock de voo em `_confirmarRemocao`** *(Source: `member_detalhe_sheet.dart:719-754`)* — sheet interativa durante `await setMembership`; duplo disparo possível (dialogs têm `_isSubmitting`, a remoção não). **Adversarial.**
14. **Esc/back fecha dialog durante submit (sem `PopScope`)** *(Source: `trocar_papel_dialog.dart:114-162`; `trocar_cargo_dialog.dart:85-119`)* — dispose mid-mutation pula invalidação/snackbar e pode tocar `ref` pós-dispose. **Edge.**
15. **Payload `modules: []` no desativar vs preservado no remover** *(Source: `member_detalhe_sheet.dart:633` vs `:728`)* — inconsistência para o mesmo `isActive:false`; reativação posterior nas condições erradas perde módulos. **Adversarial.**
16. **Diálogo "Desativando..." sem timeout e com context do sheet** *(Source: `member_detalhe_sheet.dart:599-669`)* — CF pendurada + `PopScope(canPop:false)` = modal preso; pop depende do context emissor. Liga-se ao EC2 já deferido (checkConnectivity-hang). **Edge+adversarial** → defer com EC2.
17. **Raio `cachedList`: invalidar stream pode re-emitir cache em disco antes do ack do servidor** *(Source: `read_cache.dart:124-148`; invalidações em `trocar_papel_dialog.dart:137-143`, `member_detalhe_sheet.dart:732-737`)* — flash da linha removida. **Não verificado em runtime** — defer com settle (evidência), não vira defeito estabelecido.
18. **Race multi-admin: confirm tardio de Trocar papel reativa vínculo removido** *(Source: `trocar_papel_dialog.dart:134` — `isActive: true` fixo)* — baixa probabilidade, sem guarda de versão. **accept/defer.**

**Gaps de verificação (verification-gap — asserções ausentes; testes verdes não cobrem):**

19. **`setCargo` real sem teste** — só `FakeMembrosRepository` (`membros_test.dart:76-101`); a 9.2 tinha testes do repo real (`:2574-2626`). Pré-check offline/payload/catch do método real não verificados. *(regression-gap)*
20. **Wiring do sheet nunca dirigido para Trocar cargo** — nenhum teste toca o botão da sheet com `trustedDev` override nem asserção `Cargo atualizado.` (grep: zero hits); `cargoAtual`/`isDev` injetados direto no dialog. *(regression-gap)*
21. **Prefill via overflow não assertado** — teste de wiring só checa `find.byType(TrocarPapelDialog)` (`:2935-2955`); params pré-preenchidos testados isolados. *(regression-gap)*
22. **Invalidação de providers nunca assertada** — zero testes observam rebuild pós-mutação; apagar qualquer bloco `ref.invalidate` fica verde. *(regression-gap)*
23. **N+1 e progresso não testados com 2 obras confirmadas** — `nObras: 2` só lista nomes (`:3224-3246`); happy path de CF é `nObras: 1` (`:3274-3304`); `Desativando...` sem teste. Regressão N→1 passaria. *(regression-gap)*
24. **A11y dos surfaces novos sem asserção** — Esc/foco/≥48dp/`Editar vínculo`/textScale 1.3 não testados nos dialogs 10.x e no overflow (só existe Esc do sheet 8.3, `:1747`). Pares do finding 12. *(regression-gap)*

## Behavior verification

- **Re-executada nesta retrospectiva (2026-09-22):**
  - `flutter analyze` — **No issues found!** (exit 0).
  - `flutter test test/features/construtoras/membros_test.dart` — **110/110 All tests passed!** (exit 0; eram 84 no fim do épica 9 → +26 testes).
- **Evidência de verificação nas specs (não re-executada além da acima):** specs 10.1/10.2 e 10.3 declaram analyze + a mesma suite.
- **Runtime/E2E manual:** **não exercitado** (escopo estreitado: sem Firebase emulado/dispositivo nesta run) — revogação de acesso, gate `active(cm)` e fluxo real de desativação N+1 ficam *nunca checados em runtime*.
- **Backend:** `functions/` intocado no range; testes de functions não re-executados (fora do escopo de mudança).

## Previous-retro follow-through

Retro anterior direta: `epic-9-retro-2026-09-22.md` (3 itens open). Nenhum item anterior tem evidência de conclusão no range do épica 10; dois **pioraram**. **Sem transições propostas** para `--set-action-status`.

| id | landed? | status argued |
|----|---------|---------------|
| `epic-9-retro-item-1-reconciliar-ac-9-2` | não — `epics.md` intocado no range (diff vazio); atalho Ativar não implementado no épica 10 | nothing (open) |
| `epic-9-retro-item-2-unificar-rotulos-papel` | **não — piorou**: épica 10 adicionou `_roleLabels` em `trocar_papel_dialog.dart:71` e `trocar_cargo_dialog.dart:64` em vez de derivar de `rotuloPapelObra` | nothing (open; escopo cresceu) |
| `epic-9-retro-item-3-settle-checkconnectivity-hang` | não — `deferred-work.md` intocado; `setCargo` acrescentou o **3º** call site de `checkConnectivity` sem timeout (`membros_repository.dart:105`) | nothing (open; superfície cresceu) |
| `epic-8-retro-item-1-extrair-filtros-screen` | não — `membros_screen.dart` untouched no range (guarda segue de pé para o próximo escritor) | nothing |
| `epic-8-retro-item-2-status-chip-duplicado` | **não — arquivo tocado sem resolver**: `obra_vinculo_row.dart` +43 no range (overflow) mantém tokens inline duplicados (`:61-63`) vs `_statusChip` (`member_detalhe_sheet.dart:343-376`) | nothing (open) |
| `epic-8-retro-item-3-manter-defer-offline-sheet` | parcial — E3 ainda em `deferred-work.md:74-76` ("manter" cumprido; "resolver" segue open) | nothing (sem transição parcial) |
| `epic-5-retro-item-1-performance-360` / `epic-5-retro-item-2-e2e-anexos` | no evidence in range | nothing |
| `epic-4-retro-item-1-modularizar-chamada-form` / `epic-4-retro-item-2-fila-offline-rh` | no evidence in range | nothing |
| `epic-2-retro-item-2-teste-e2e-offline` | no evidence in range | nothing |
| `epic-1-retro-item-2-auditoria-dev-prod` + legacy epic-1 open | no evidence in range | nothing |

## Action items

Itens **propostos** para gravar em `sprint-status.yaml` como `open` (via script `sprint_status.py` na Fase 5 — nada é aplicado no código por esta retro):

- `[ ]` **epic-10-fix-cargo-reativacao-owner** (owner: `maranduteam`) — (i) habilitar `Trocar cargo` apenas para membro **ativo** (paridade com Atribuir/Desativar) ou enviar `isActive` explícito, para o backend não reativar silenciosamente (`index.ts:103`); (ii) remover fallback `owner→operario` em `trocar_cargo_dialog.dart:79-82` (bloquear confirmação quando `cargoAtual` fora de `_rolesDisponiveis`). *(findings 6, 7 — defect)*
- `[ ]` **epic-10-fix-desativar-partial-invalidation** (owner: `maranduteam`) — Endurecer `_confirmarDesativar`: (i) guard para alvo `owner` sem `trustedDev` (hoje N obras desativam e o `setCargo` final cai em `permission-denied`, `index.ts:96`); (ii) mover `ref.invalidate` para `finally` + invalidar `obraMembersProvider` de cada obra afetada; (iii) alinhar payload `modules` (preservar `vinculo.modules` como no remover, não `const []`). *(findings 9, 15 — defect)*
- `[ ]` **epic-10-fix-desativar-lista-obras** (owner: `maranduteam`) — Não abrir o confirm de Desativar com `obrasVinculadas` degradada (usar `meta.carregando/erro` já em escopo) e renderizar a branch `"Nenhuma obra vinculada."` hoje morta (`member_detalhe_sheet.dart:546-576`). *(findings 10, 11 — defect + spec I/O)*
- `[ ]` **epic-10-fix-a11y-overflow** (owner: `maranduteam`) — Overflow ≥48dp e mover `PopupMenuButton` para fora do `excludeSemantics` (com label `Opções de vínculo de {obra}`); decidir alvo dos `FilterChip` (≥48dp ou paridade `CheckboxListTile` do AtribuirObra). *(findings 12, 24; spec 10.1 Always)*
- `[ ]` **epic-10-fix-dialog-guards** (owner: `maranduteam`) — `PopScope(canPop: !_isSubmitting)` nos dois dialogs; lock anti-duplo-disparo em `_confirmarRemocao`; diálogo de progresso com context próprio + `timeout` no `Future.wait` (absorve/settle o EC2 junto com `epic-9-retro-item-3`). *(findings 13, 14, 16)*
- `[ ]` **epic-10-fix-prefill-normalizar-modulos** (owner: `maranduteam`) — Pré-preenchimento do `TrocarPapelDialog` via `normalizeModule`/`normalizeRawModules` (`core/contracts.dart`), fechando a AC 10.1 "módulos normalizam legados". *(finding 8 / 5c — spec reconciliation + defect)*
- `[ ]` **epic-10-tests-fechar-gaps-10x** (owner: `maranduteam`) — Bateria de testes: (a) `setCargo` **real** (offline + permission-denied, padrão 9.2); (b) wiring do sheet (tap `Trocar cargo` com `trustedDev` override, prefill via overflow, snackbar `Cargo atualizado.`); (c) asserção de invalidação pós-mutação; (d) desativar com `nObras: 2` confirmando N+1 + `Desativando...`; (e) a11y 10.x (Esc, ≥48dp, `Editar vínculo`, textScale 1.3). *(findings 19-24)*
- `[ ]` **epic-10-spec-reconcile-epics** (owner: `maranduteam`) — Reconciliar `epics.md`: AC 10.3 (texto do confirm ≠ `Remover de N obras + desativar?`) e decidir o carryover da AC 9.2 (atalho Ativar — implementar ou remover; AD-9 ainda o declara). *(findings 5a, 5e — spec reconciliation, decisão humana)*
- `[ ]` **epic-10-dedup-erros-rotulos-tests** (owner: `maranduteam`) — Consolidação: extrair tradução/constantes de erro compartilhadas (`setCargo` vs `setMembership`), unificar rótulos de papel (absorve o escopo de `epic-9-retro-item-2`) e status-chip (escopo de `epic-8-retro-item-2`); **fatiar `membros_test.dart`** por story/feature — 3ª retro seguida emitindo essa lição. *(findings 2, 3; lição de processo recorrente)*

Lição de processo (sem item extra): subjects de commit não carregam as chaves do sprint-status (atribuição epic-wide só manual) — nomear commits com o id da story melhora a prova futura; e specs `route: dispatch` devem evitar reformat de código no mesmo commit da spec (`4ee0852`).

## Acceptance verdict

- **Verdict:** `accepted-with-open-items` — **override humano explícito** sobre a leitura estrita da máquina (`rejected`: I/O "sem obras" não exibida + prefill sem normalização + reativação de inativo). O humano aceitou os critérios principais como cumpridos com os 9 open items acima rastreados.
- **Criteria:** `declared` — ACs Given/When/Then em `epics.md` (Stories 10.1–10.3) + I/O matrix das specs congeladas; FR6–FR9.
- **Evidência a favor:**
  - `pending_stories = []` (`detect-epic --epic 10`); tasks `[x]` nas duas specs.
  - Verificação re-executada: analyze 0 issues, **110/110 testes verdes** (+26 vs épica 9).
  - Arquitetura aderente AD-1/AD-3/AD-7/AD-8; sem escrita direta; backend intocado e `owner` protegido por `dev` (`index.ts:96`).
  - Fronteiras principais de review viraram itens com fonte; nenhum defeito destrói os fluxos happy-path declarados.
- **Evidência contra (critérios declarados não plenamente cumpridos):**
  1. I/O 10.3 "sem obras → lista *nenhuma obra vinculada*" nunca exibida (branch morta, finding 5b/11).
  2. AC 10.1 "módulos normalizam legados" ausente no prefill do diálogo (finding 5c/8) — confirmar sem alterar pode revogar acesso legado.
  3. Defeito além dos ACs: reativação silenciosa de membro inativo via Trocar cargo (finding 6) — viola a intenção do ciclo de vida "desativar".
- **Histórico de decisão:** `accepted-with-open-items` confirmado pelo usuário (maranduteam) nesta run; máquina apontaria `rejected` (3 pendências acima) — humano sobrescreveu (regra 1).

## Open questions

- O texto do confirm de desativação deve seguir o `epics.md` (`Remover de N obras + desativar?`) ou o código (`Desativar {email}?` + lista) vira a verdade e o `epics.md` é reconciliado?
- Atalho Ativar (carryover 9.2/AD-9): implementar (onde — sheet, dialog `failed-precondition`?) ou remover da AC/AD? Enquanto aberto, a AC descreve comportamento inexistente.
- `FilterChip` <48dp: é restrição visual aceita (documentar waiver) ou migrar para alvos 48dp/`CheckboxListTile`?
- Flash de `cachedList` pós-invalidação (finding 17): confirmar com rodada manual/offline ou teste de stream com cache — só então promover de "nunca checado" a defeito/limpo.
- Sessões 10.x sem session logs (mesma lacuna dos épicas 8/9): registrar processo nas próximas stories ajudaria retros futuras?

## Assumptions

*(run interativo — escolhas confirmadas pelo usuário registradas na Epic summary acima; sem seção de suposições headless.)*
