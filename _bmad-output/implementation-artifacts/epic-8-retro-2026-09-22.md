---
epic: 8
date: 09-22-2026
verdict: accepted-with-open-items
criteria: declared
headless: false
---

# Retrospectiva — Epic 8: Visibilidade dos vínculos

## Epic summary

- **Epic:** 8 — Visibilidade dos vínculos (FR1, FR2, FR3 de `epics.md`).
- **Diff range:** `5d82e2b^..749bc7e` (parent do primeiro commit da story 8.1 até o aceite final da 8.3). 7 commits, 0 merges.
- **Stories concluídas (todas `done`):**
  - `8-1-lista-com-cargo-n-obras` — baseline `bd223c4`
  - `8-2-filtros-e-busca` — baseline `6f0f2a0`
  - `8-3-detalhe-do-membro` — baseline `39ef704`
- **Pending stories:** nenhuma (`pending_stories = []` via `detect-epic --epic 8`).
- **Volume de código (não-merge, top churn):**
  - `app/test/features/construtoras/membros_test.dart` +1691 net
  - `app/lib/src/features/construtoras/presentation/member_detalhe_sheet.dart` +428 (novo)
  - `app/lib/src/features/construtoras/presentation/membros_providers.dart` +373 net
  - `app/lib/src/features/construtoras/presentation/membros_screen.dart` +356 net
  - `app/lib/src/features/construtoras/presentation/widgets/obra_vinculo_row.dart` +124 (novo)
  - `role_chip.dart` +108 (novo), `member_row.dart` +99, `obra_repository.dart` +20
- **Inventário de evidências:**
  - Epic context: `epic-8-context.md` (presente).
  - Specs de story: `spec-8-1-…`, `spec-8-2-…`, `spec-8-3-…` (presentes, status `done`, com Review Triage/Findings).
  - Architecture: `_bmad-output/planning-artifacts/architecture/architecture-obras-2026-09-21/ARCHITECTURE-SPINE.md` (AD-1…AD-9).
  - UX: `ux-designs/ux-obras-2026-09-21/DESIGN.md` e `EXPERIENCE.md`.
  - Epics/ACs: `planning-artifacts/epics.md` (Stories 8.1–8.3 com Given/When/Then).
  - Sprint status: `sprint-status.yaml` (epic-8 + retrospectiva `optional`).
  - Retro anterior: `epic-5-retro-2026-09-21.md` (e retors 1/2/4).
  - **Session logs / memlogs das stories 8.x:** ausentes — análise de processo pulada (escopo estreitado; evidência só specs + commits + reviews registrados).
  - Diffs por story (`diff-spec-8-*.diff`): ausentes — range epic-wide via `git_evidence.py` usado no lugar.
  - PRD: ausente — critérios vêm das ACs das stories e FRs de `epics.md` (**declared**, não profiled-only).

## Findings

### Aggregate views

1. **Architecture delta — aderência AD-5 / sem escrita** *(Source: ARCHITECTURE-SPINE.md:63-67; diff range; specs Never)*
   - O epic manteve agregação no cliente (N `watchObraMembers` + junção em memória), sem `collectionGroup`, sem escrita (`setMembership` fora do escopo), rota com `AccessGuard(adminOnly:true)` intacta. Imports do range ficam em `construtoras/`, `obras/` domain/data e widgets comuns — sem camadas cruzadas novas.
   - **Disposition:** accept.

2. **Duplication map — status chip Ativo/Inativo** *(Source: `member_detalhe_sheet.dart:319-352`; `obra_vinculo_row.dart:52-116`)*
   - Tokens de status (bg/borda/ink/ícone Ativo vs Inativo) aparecem em dois widgets quase idênticos; `rotuloStatusVinculo` já é compartilhado, mas o `Container` de chip não. Risco baixo de divergência visual se um lado mudar.
   - **Disposition:** defer (refactor opcional se a superfície de status crescer no epic 10).

3. **God-class / crescimento — `membros_screen.dart`** *(Source: `git_evidence` files; `wc -l` = 514)*
   - Tela concentra lista + filtros + busca + dropdown + refresh + estados de erro/offline. Crescimento previsível do epic, ainda abaixo de limite crítico; testes da suíte (1693 linhas) acompanham.
   - **Disposition:** accept + lição de processo: extrair barra de filtros se o epic 9 acrescentar fluxos de escrita na mesma tela.

4. **Pattern divergence — provider do detalhe** *(Source: `membros_providers.dart:277-281`; conventions spine :99)*
   - `memberDetalheProvider` é `FutureProvider.autoDispose.family` (leitura pontual via `getMember`) enquanto o resto da feature é majoritariamente `StreamProvider` — intencional para doc único; naming segue `…Provider` da convenção.
   - **Disposition:** accept.

5. **Spec-to-implementation — UX ASSUMPTION painel lateral >1200px** *(Source: DESIGN.md:87; spec-8-3 Boundaries; código usa Dialog 480px)*
   - ASSUMPTION de painel lateral desktop não implementada; caminho adotado (Dialog 480px ≥800px) é o do Intent congelado. Decisão humana na spec 8.3 (ações placeholder, CTA vazio estático) respeitada.
   - **Disposition:** accept (deviation registrada; spec já documenta).

6. **Fronteira 8.1→8.2→8.3 — estados vazios/falsos** *(Source: spec-8-2 Review P1–P9; spec-8-1 B1–B11; spec-8-3 triage)*
   - Padrão recorrente: `.value ?? []` / agregação parcial virando “vazio” durante loading/erro. Cada story corrigiu após review; lição cruzada: **AsyncValue nunca colapsa para lista vazia sem distinguir meta**. Patches das três stories aplicados e verificados (analyze 0, membros_test 67/67).
   - **Disposition:** accept (corrigido); lição de processo para epics seguintes.

### Diff-scope review (fronteiras entre stories)

Reviews por story já executadas com múltiplas lentes (8.1: 9 patches; 8.2: 9 patches P1–P9 + 4 revisores; 8.3: 14 patches + 1 defer). Nesta retrospectiva o foco foi o **intervalo epic-wide** e as fronteiras entre sessions — não houve nova rodada `bmad-review` independente (escopo estreitado: consolidar findings já triados nas specs + análise de agregados). Findings ainda abertos/deferidos:

- **Defer E3 — loading infinito do sheet offline** *(Source: `spec-8-3` triage E3; `deferred-work.md:74-76`)* — `contagemObrasMetaProvider.carregando` sem emissão mantém spinner sem timeout. Registrado; settles com teste de stream não-emitting ou evidência de cache offline.
- **Limitação pré-existente SigoTopBar** *(Source: spec-8-2 Implementation Notes)* — overflow 42px em 360px/1.3 no `AppBar` global, fora do escopo das 9 correções; componente não alterado.
- **Sem findings novos de fronteira não tratados** nas specs com veredito medium+ que não estejam patch/defer.

## Behavior verification

- **Evidência de verificação nas specs (não re-executada nesta retro):**
  - `flutter analyze` — No issues found (exit 0) [8.1/8.2/8.3].
  - `flutter test test/features/construtoras/membros_test.dart` — **67/67 All tests passed** (após patches 8.2/8.3); 8.1 também roda `owner_atribuicao_test`.
- **Runtime/E2E manual:** **não exercitado** nesta retrospectiva (sem emulador/dispositivo no escopo). Escopo estreitado: confiança baseada em suite de widget tests + reviews registradas.
- **Produção:** não consultada / não implantada por este epic (somente leitura).

## Previous-retro follow-through

Retro anterior direta: `epic-5-retro-2026-09-21.md` (itens open em `sprint-status.yaml`). Nenhum item anterior tem evidência de conclusão no range do epic 8 (o epic não tocou 360º, E2E offline, anexos, chamada RH, etc.). **Sem transições propostas** para `--set-action-status`.

| id | landed? | status argued |
|----|---------|---------------|
| `epic-5-retro-item-1-performance-360` | no evidence in range | nothing |
| `epic-5-retro-item-2-e2e-anexos` | no evidence in range | nothing |
| `epic-2-retro-item-2-teste-e2e-offline` | no evidence in range | nothing |
| `epic-4-retro-item-1-modularizar-chamada-form` | no evidence in range | nothing |
| `epic-4-retro-item-2-fila-offline-rh` | no evidence in range | nothing |
| `epic-1-retro-item-2-auditoria-dev-prod` + itens legacy epic 1 open | no evidence in range | nothing |

## Action items

Itens **propostos** (não aplicados automaticamente):

- `[ ]` **epic-8-retro-item-1-extrair-filtros-screen** (owner: `maranduteam`) — Se o epic 9 acrescentar escrita em `membros_screen.dart`, extrair barra de filtros/busca/dropdown para widget dedicado antes do próximo crescimento. *(lição: god-class growth, finding 3)*
- `[ ]` **epic-8-retro-item-2-status-chip-duplicado** (owner: `maranduteam`) — Opcional: unificar tokens do chip Ativo/Inativo entre `MemberDetalheSheet._statusChip` e `ObraVinculoRow` num helper compartilhado. *(finding 2; defer)*
- `[ ]` **epic-8-retro-item-3-manter-defer-offline-sheet** (owner: `maranduteam`) — Manuter E3 (`offline-loading-sheet`) em `deferred-work.md` e resolver com teste de stream não-emitting quando houver orçamento de risco offline no sheet. *(já deferido; não duplicar código agora)*

Lição de processo (sem item de código): em stories futuras de UI assíncrona, **proibir `.value ?? []` como fallback de render** sem ramo explícito de erro/loading — padrão que dominou os findings das três stories.

## Acceptance verdict

- **Verdict:** `accepted-with-open-items`
- **Criteria:** `declared` — ACs Given/When/Then em `epics.md` (Stories 8.1–8.3) e nas specs; FR1–FR3 cobertos.
- **Evidência:**
  - `pending_stories = []` (detect-epic --epic 8).
  - Tasks de execução `[x]` nas três specs; verification analyze/teste verde documentado.
  - Fronteiras de review fechadas com patches aplicados (8.1: 9; 8.2: P1–P9; 8.3: 14 + defer).
  - **Open items nomeados:** defer E3 (offline loading sheet); limitação SigoTopBar pré-existente; itens de refactor opcional (status chip, extração de filtros) — nenhum bloqueia o critério do epic (leitura, sem escrita, a11y e microcopy conforme Intent).

## Open questions

- E3 (sheet offline): qual o comportamento real do `cachedRead`/streams do Firestore no cold start offline sem cache — define se o defer vira fix ou se o timeout é desnecessário?
- Painel lateral >1200px (ASSUMPTION UX): vale retomar no epic 10 quando houver ações de escrita no detalhe, ou Dialog 480px é permanente?
- Sessões 8.x sem session logs: registrar processo nas próximas stories ajudaria retrospectivas futuras a separar “não checado” de “checado e limpo”?
